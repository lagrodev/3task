# mathematics-Library

Библиотека для работы с линейной алгеброй. Есть пожелания - пишите)

Так, ну по базе. Данная библиотека, вроде как, сделана по принципам ООП. Чего ей/мне не хватает для полного счастья? Пойдем по порядку:

- увы, но нет возможности называть методы одинаково
- Нет возможности не делать громоздких операций, по типу сейчас, да и потом, это реализовано так:

```
Vector3f first = new Vector3f(1,2,3);  
Vector3f second = new Vector3f(2,3,4);  
// я хочу сложить 2 вектора  
res = first.add(second);  
// как бы отстой. я хочу так:  
res = first + second;  
// но наша любимая java не хочет давать мне право переопределять арифмитические операторы  👍  
```

- К сожалению. я передумал ее делать закрытой и хешировать внутренности :( (нет, не то, чтобы мне стало впадлу понимать, как это делается, скорее я понял, что смысла особо нет - так особо ни у кого не будет интереса чекать мою библиотеку, хах)
- тесты - пока прописаны не все. в будущем пропишу еще, наверное.
- Из-за 3го пункта возможны баги) Буду благодарен - если что-то найдете.
- Если вы поймете как уйти от `createInstance` и `instantiateVector` - я буду счастлив, напишите мне :)

# Краткий функционал

Библиотека состоит из 2х главных классов, в которых прописана основная логика для матричных преобразований - `AbstractMatrix` и `AbstractVector`. От этих классов унаследовано все остальное.

## **ВАЖНО**

Если вы хотите добавить что-то свое - по типу, `Vector5f`  - то вам необходимо в данном классе указать длину вашего вектора, т.е.

`private static final int SIZE = 5;`

То же самое касается матриц!

Я пока еще не придумал, как автоматизировать все это дерьмо - чтобы SIZE как-то сам прописывался... ладно, потом подумаю (нет). Опять же, если знаешь как - напиши)

## Основные операции

### `AbstractVector`

#### Конструкторы

```
Вы можете спросить, зачем ты выдлелил это в отделльный пункт, однако это мой проект, так что, МОГУ СЕБЕ ПОЗВОЛИТЬ :)   
```

Если серьезно - в данный момент по дефолту прописаны след. Конструкторы:

```
public AbstractVector(float... components) {}  
public AbstractVector() {} // есть шанс, что в будущем я удалю данный метод :)  
```

В чем прелесть и почему я рекомендую вам пользоваться именно моими классами, т.е. для `Vector5f` делать так:

```
public Vector5f(float... components) {  
super(components);  
}  
public Vector5f() {  
super();  
}  
```

Таким образом вы получите возможность задавать вектор сразу несколькими способами:

```
Vector3f first = new Vector3f(1,2,3);  
int[] a = {1,2,3}  
Vector3f second = new Vector3f(a);  
```

Тема классная - рекомендую)
`AbstractVector()` - создает нулевой вектор.

#### Сложение

Реализовано 2 метода сложения (в теории, можно было сделать 4, но я смысла не вижу), а именно:

```
public AbstractVector add(AbstractVector other){}
public void addV(AbstractVector other){}
```

Пойдем по глобальной разнице и почему этих методов 2?!

- `add` - создает новый вектор, благодаря этому можно в 1ну строчку делать несколько методов.
- `addV` меняет текущий вектор (вот пример кода, который демонстрирует главную разницу)

```
Vector3f v1 = new Vector3f(5, 7, 8);  
Vector3f v2 = new Vector3f(2, 3, 3);  
Vector3f result = v1.add(v2).normalizeV();
// если использовать метод addV, то надо это делать в несколько строк:
Vector3f v1 = new Vector3f(5, 7, 8);  
Vector3f v2 = new Vector3f(2, 3, 3);  
v1.addV(v2); // тут мне java не позволяет прописать вызов функции normalizeV() или // normalize()
v1.normalize();
```

Как можно увидеть, мало того. что addV мне не позволил создать новый вектор, так я еще и не могу использовать вызов функции в той же строке. Зачем тогда сохранять метод `addV`? Все просто - а всегда ли нам не надо менять состояние текущего вектора? мне кажется, порой в задаче требуется поменять первоначальный вектор, чтобы не создавать кучу других - не нужных)
Чуть не забыл:

<p>
Мем, как работает метод, который возвращает AbstractVector - Я пипец, какой ленивый, поэтому в нем я просто создаю новый вектор, с такими же параметрами, как исходный и просто для него делаю вызов метода void :)

#### Вычитание

Реализовано 3 метода вычитания (в теории, можно было сделать 4, но я смысла не вижу.. я даже хз, нужен ли последний метод, он мне не нравится), опишу основные и те, которыми рекомендую пользоваться, а именно:

```
public AbstractVector sub(AbstractVector other){}
public void subV(AbstractVector other){}
```

- тут точно так же, что и со сложением... скорее всего, дальше я только и буду что писать: чекни сложение, чекни то-то... мне лень 1000000 раз писать одно и тоже...
- метод `public void sub(AbstractVector first, AbstractVector second) {}` - я не понимаю зачем он, и не помню под чем его писал, поэтому рекомендую пользоваться первыми двумя, спасибо за понимание)

#### Умножение (multiply) и деление (divide) на скаляр

Принцип работы, как и со сложением. 2 метода, один void, другой AbstractVector - разница - в названии, на этот раз void - это тот, который без V (я когда их прописывал, у меня уже был создан void метод, и чет исправлять мне стало лень)
При делении. в теории, можно было прописать что-то по типу:

```
public void divide(float scalar) {  
    if (scalar == 0) throw new ArithmeticException("Деление на ноль");  
	this.multiply(1/scalar);  
}
```

Почему я так не сделал? Опять же, я писал изначально все раздельно + торопился... потом все переделывал 100 раз и исправлять это стало впадлу... хотя, пожалуй, не не буду править - лень

#### Скалярное произведение

Че такое скалярное произведение? эта штука возвращает число - в отличии от векторного произведения.

```
public float dot(AbstractVector other) {}
```

Как работает?
Просто покомпонентно перемножает 2 вектора, чекни код, чтобы понять, я пас расписывать

#### Нормализация

Так, опять 2 метода - void и AbstractVector. Разницу вы уже знаете. Процитирую, что я написал в коде:

<p>
Нормализует вектор. При этом изменяет состояние текущего вектора. из-за этого нельзя делать пару приколюх...  </p>
<p>p.s. рекомендую использовать 2й метод (в целом, вам решать что делать)</p>

Если длина вектора равна нулю, метод ничего не делает, иначе делит на длину - можно конечно было прописать так:

```
if (length == 0 || length==1) {  
    return;  
}
```

но опять же, мне впадлу + работает - не трожь

О осталось ластовое, УРА

#### getNum и setNum

Возвращает/ меняет эл-т по индексу. Все, больше ничего не скажу.

### Реализация классов детей - Vector2f, Vector3f и Vector4f

В чем главная разница и что у них нового?

1) Присутствуют методы `equals`, `hashCode` и `toString` - для сравнения 2х векторов и представление в строчном виде. В теории, можно было бы это засунуть в класс родителя, но я не придумал как + мне уже так впадлу стало что-то менять
2) Многие методы - Вопрос, зачем я их прописал? Ответ прост: я манал, что мне java просит прописывать вот так, без методов:

```
Vector2f result = (Vector2f) v1.add(v2);
 // вместо этого:
 Vector2f result = v1.add(v2);
```

Поэтому, я согласился прописать это все. В вашем классе это делать не обязательно
3) Vector3f - содержит метод `cross(Vector3f other)`  и `crossV(Vector3f other)` для векторного произведения - почему не в родителе? все просто - этот метод уникален и может быть использован только для вектора 3f
Все, с векторами покончили - Ура, победа

### `AbstractMatrix`

#### Конструкторы

В данный момент по дефолту прописаны след. Конструкторы:

```
public AbstractMatrix(float... array) {}  
public AbstractMatrix(int one) {} // единичная матрица
public AbstractMatrix() {}  // нулевая матрица
public AbstractMatrix(float[][] array){}
public AbstractMatrix(AbstractMatrix elements) {}
```

Методы `createInstance` и т.п. нужны для внутренней реализации. так они бесполезны.
Так, из важного - `size` - размер матрицы. ОНА ОБЯЗАТЕЛЬНО КВАДРАТНАЯ!!!!
Методы:

#### Сложение и вычитание

Как и в векторах, читай выше

#### Транспонирование

Опять 2 метода

```
public void transposition() {}
public AbstractMatrix transpositionNew(){}
```

Разница понятна

#### Умножение

##### Умножение матрица на вектор

Я умножаю матрицу на вектор, т.е. A*B = C

* Где А - матрица, B- вектор , С - вектор
* Возвращаю вектор, т.к. работаю с векторами столбцами.
* В конце использую метод `instantiateVector()` - чтобы получить вектор... увы, я не придумал чет лучше, чем этот костыль :(

##### Умножение матрица на матрицу

2 метода - void и обычный:

```
public void multiply(AbstractMatrix other) {}
public AbstractMatrix multiplyNew(AbstractMatrix other){}
```

Метод умножения матрицы на матриц, т.е. A*B = C

* Где А - матрица this, B - oter, С - матрица
* Короче, лень писать, как оно работает - читай в коде

### Поиск детерминанта

Изначально я не придумал, как это все делать, поэтому этот ужас был прописан отдельно для матрицы 3f и 4f - было страшно и ужасно. Потом я покумекал и кое-что понял)
Сделал с помощью рекурсии - передавать стал не Matrix, а массив ее эл-тов. + длина текущей матрицы - от последнего кнш можно избавиться, но я оставил)

+ Для подсчета новой матрицы использую подсчет минорной матрицы) Потом это пригодится в поиске обратной матрицы)

### Поиск обратной матрицы (inverse)

Тут я намучался, т.к. долго не мог понять, как все это сделать правильно - проблема в том, что я везде стремился работать не с 2мерным массивом, а с объектом матрицы, из-за
этого столкнулся с ужасной проблемой - у меня нет класса, описывающего Matrix2f, для поиска минорной матрицы + я покумекал и понял, что в теории, кто-то может создать класс Matrix6f и хочет посчитать обратную матрицу. Как ему это сделать? если я передаю матрицы, то ему надо создать класс Matrix5f - больше проблем + я не придумал, как сделать код таким образом, чтобы можно было передавать матрицы разных размерностей... короче, куча тяжелых проблем
Их решение оказалось банальным :)
Что такое матрица? Это двумерный массив. Т.е. почему бы мне не играться с двумерными массивами?! Казалось бы, банально? Но нет, я потратил на это почти 2е суток, ахах
Ну, ладно, забили, дальше

#### hashCode() и toString()

Прописаны в родителе, т.к. они одинаковы.

### Дети - Matrix3f и Matrix4f

Все одинаково, как и в родители, новое - метод `equals`
